import { Preloader } from '@/components/ui/preloader';

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  // For this case, we'll reuse the Preloader component.
  return <Preloader />;
}
